{#
/**
 * @file
 * Default theme implementation for an 'longitude' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_longitude()
 *
 * @ingroup themeable
 */
#}
<input{{ attributes }} />{{ children }}